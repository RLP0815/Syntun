layui.use(['element','form','layer','table','laytpl'],function(){
	
	var element = layui.element;

	var form = layui.form,
        layer = parent.layer === undefined ? layui.layer : top.layer,
        $ = layui.jquery,
        laytpl = layui.laytpl,
        table = layui.table;

	// 点击添加新tab；
    $(".panel a").click(function(){
        parent.addTab($(this));
    })
    //搜索【此功能需要后台配合，所以暂时没有动态效果演示】
    $(".search_btn").on("click",function(){
    	//$(".searchVal").val();
    	//alert($(".searchVal").val());
    	if ($(".searchVal").val() == "IP132_xitong"){
        	$(".IP60_skyscope_etl").hide();
        	$(".IP132_xitong").show();
    	}else if($(".searchVal").val() == "IP60_skyscope_etl"){
        	$(".IP132_xitong").hide();
    		$(".IP60_skyscope_etl").show();
    	}else{
    		$(".IP132_xitong").hide();
    		$(".IP60_skyscope_etl").hide();
    	}
    });
})
